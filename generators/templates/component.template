import React from 'react'
import PropTypes from 'prop-types'

import './index.scss'

const {{ COMPONENT_NAME_HOLDER }} = props => (
  <div className="{{ NAME_HOLDER }}">{ props.message }</div>
)

{{ COMPONENT_NAME_HOLDER }}.propTypes = {
  message: PropTypes.string
}

{{ COMPONENT_NAME_HOLDER }}.defaultProps = {
  message: 'This is component {{ COMPONENT_NAME_HOLDER }}'
}

export default {{ COMPONENT_NAME_HOLDER }}